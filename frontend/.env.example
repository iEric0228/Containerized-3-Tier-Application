# API base URL for the backend.
# Local Docker Compose: leave empty (Nginx proxies /api/ → backend:3001)
# Production (ECS via ALB): leave empty (Nginx proxies /api/ → backend service)
# Direct backend access (e.g., local dev without Nginx): http://localhost:3001/api
REACT_APP_API_URL=

REACT_APP_ENVIRONMENT=